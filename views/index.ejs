<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %>
	<style>
		a:hover {
			color: #EE72F8;
		}
			
        .centered-content {
            display: flex;
            justify-content: center;
            align-items: center;
            height: calc(100vh - 56px); /* Adjust for navbar height */
            text-align: center; /* Center text content */
            padding: 15px; /* Add padding to avoid overflow */
            flex-direction: column; /* Stack elements vertically */
        }
		
		@media (min-width: 992px) {
            .centered-content {
                max-width: 50%;
                margin: 0 auto; /* Center the content horizontally */
            }
        }
		
        .text-row {
            background-color: white;
            color: black;
            padding: 15px;
            margin-bottom: 10px;
            width: 100%;
            box-shadow: 3px 3px 0 3px #EE72F8;
            text-transform: uppercase; /* All caps for all rows */
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: transform 0.3s, box-shadow 0.3s; /* Smooth transition for hover effect */
        }
        .text-row:hover {
            transform: translateY(-5px); /* Moves the row up */
            box-shadow: 3px 3px 0px 3px green; /* Changes shadow color to green */
        }
        .arrow-button {
            border: none;
            background: none;
            color: black; /* Black arrows */
            cursor: pointer;
            font-size: 1.5em;
        }
        .row-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            text-decoration: none; /* Removes underline from links */
            color: inherit; /* Inherits text color */
            width: 100%;
        }
        .row-content:hover {
            text-decoration: none;
        }
		
        #row1 {
            background-color: black;
            opacity: 0.9;
            color: white;
			box-shadow: 0 0 0 0;
        }
		
		#row1:hover {
			transform: none;
		}
		
		#row2 {
			box-shadow: 0 0 0 0;
		}
		
		#row2:hover {
			transform: none;
		}
		
		#row4 {
            background-color: grey;
            opacity: 0.9;
            color: white;
			box-shadow: 0 0 0 0;
        }
		
        /* Media query for mobile devices */
        @media (max-width: 768px) {
            .text-row {
                flex-direction: column; /* Stack content vertically */
                text-align: center; /* Center text content */
            }
            h1 {
                font-size: 2rem;
            }
            .h2, .h3, .h4 {
                font-size: 1.2rem;
            }
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    <main>
		<!-- Centered Content -->
		<div class="centered-content">
			<div class="text-row" id="row1">
				<p class="h3">Lespakket over polarisatie voor MBO Burgerschapsonderwijs</p>
			</div>
			<div class="text-row" id="row2">
				<h1  style="font-size: 5rem">Potje Polariseren</h1>
			</div>
			<div class="text-row" id="row3">
				<a href="/auth/register" class="row-content">
					<p class="h2">Begin een Potje</p>
					<button class="arrow-button">&#x2192;</button>
				</a>
			</div>
			<div class="text-row mt-5" id="row4">
				<a href="/over" class="row-content"><p class="h4">Meer informatie</p><button class="arrow-button">&#x2192;</button></a>
			</div>
		</div>
    </main>
    <%- include('partials/footer') %>
</body>
</html>