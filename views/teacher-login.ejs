<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %>
</head>
<body>
    <%- include('partials/header') %>
    <main>
        <h1>Potje Beheren</h1>
        <% if (locals.error) { %>
            <p class="error"><%= error %></p>
        <% } %>
        <form action="/auth/teacher-login" method="POST">
            <div>
                <label for="roomId">Klas ID:</label>
                <input type="text" id="roomId" name="roomId" value="<%= locals.roomId || '' %>" required>
            </div>
            <div>
                <label for="pinCode">PIN:</label>
                <input type="password" id="pinCode" name="pinCode" value="<%= locals.pinCode || '' %>" required>
            </div>
            <button type="submit">Login</button>
        </form>
    </main>
    <%- include('partials/footer') %>
</body>
</html>